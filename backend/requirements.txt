# Requires Python >= 3.10  (models use PEP 604 `str | None` in SQLAlchemy Mapped[...])
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29
sqlalchemy>=2.0,<3.0
pydantic>=2.5,<3.0
python-dotenv>=1.0
httpx>=0.27
pytest>=8.0
# Optional: PostgreSQL (Supabase). Local dev falls back to SQLite via DATABASE_URL.
# psycopg2-binary>=2.9
