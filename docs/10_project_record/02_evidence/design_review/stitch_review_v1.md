# Stitch 와이어프레임 1차 리뷰 (9장)

- 리뷰 일시: 2026-09-03
- 대상: https://stitch.withgoogle.com/projects/11077511219977569889
- 기준 문서: `docs/CONTRACT.md` v3.0
- 리뷰어: 장병헌(지휘) 확인용 / 대조 수행: 오케스트레이터
- 판정: **9장 중 8장 재생성 필요.** 로그인 화면만 그대로 사용 가능.

## 근거 (실제 코드 확인)

| 확인 대상 | 명령 | 결과 |
|---|---|---|
| 회원가입 존재 | `grep -rn signup backend/app` | `POST /auth/signup` 존재 → 로그인 화면의 "회원가입" 링크 **정상** |
| 엔지니어 KPI | `backend/app/schemas/dashboard.py` | `draft·aiRunning·pending·rejected` **4종뿐** |
| 안전관리자 KPI | 동일 파일 | `pending·todayProcessed·monthApproved·monthRejected` + `rejectReasonsTop` |
| 승인율·완료율 | 동일 파일 | **필드 없음** |

## S급 — 고치지 않으면 발표에서 터진다

| # | 화면 | 문제 | 근거 |
|---|---|---|---|
| S-1 | 1·2·3 | 브랜딩이 전부 `ReplaceFlow`. 로고 `RF`, 계정 `engineer@replaceflow.test` | 프로젝트명 Argus로 변경됨 |
| S-2 | 5·7 | **역할이 뒤바뀜.** 화면은 엔지니어(`엔지니어 메인`, `내 요청 목록`)인데 헤더는 안전관리자 이정호 + 안전관리자 내비 | CONTRACT §3 역할 분리 |
| S-3 | 6 | `가스캐비닛 설비 모니터링 / LINE A2-GA-04 / 1.24 MPa / 상세 센서` — **센서·IoT 연동 기능이 없다** | 코드에 해당 모듈 없음 |
| S-4 | 6 | `체크리스트 사전 검증 완료 (0건 차단) / 안전 규정 7개 점검 항목` — 체크리스트는 범위에서 제외됨 | 같은 화면 상단에 "체크리스트 blocking 없음"이라 적혀 자기모순 |
| S-5 | 2 | `실시간 스트리밍 중` · `LIVE CONNECTION` — 실제는 2.5초 폴링 | CONTRACT §6, SSE는 Phase 2 |
| S-6 | 2 | `Worker node: worker-us-west-gpu#04` — 존재하지 않는 인프라 | 국내 반도체 팹 시나리오에 us-west GPU 노드는 설명 불가 |
| S-7 | 4 | `실시간 서명 연동` — 전자서명 기능 없음 | 승인은 승인/거절+사유뿐 |
| S-8 | 1 | `OCR_READY` 배지 — OCR 없음 | 사진은 저장·조회만 |
| S-9 | 7 | `승인 완료율 83.3%` — 스키마에 없는 필드이고 **산수도 틀림** (승인 1건 / 전체 6건 = 16.7%) | `dashboard.py` |
| S-10 | 6 | `승인율 50.0% 누계`, `목표 처리율 100% 달성` — 둘 다 없는 필드 | `dashboard.py` |

## A급 — 계약 불일치

| # | 화면 | 문제 |
|---|---|---|
| A-1 | 2 | A1 완료 `오후 8:01:20`, A2 완료 `오후 3:29:35` → **A2가 A1보다 먼저 끝난 것으로 표시.** 순차 파이프라인이므로 A1 < A2 < A3 여야 한다. 콘솔 트레이스(`15:29:32→34→36`)와도 어긋난다 |
| A-2 | 1 | 기본 정보의 `운전 조건·압력`과 제품 정보의 `압력 등급`이 플레이스홀더까지 `예: 3000 psi`로 동일 → 같은 값을 두 번 받는 것처럼 보인다. 전자는 설비 운전 압력, 후자는 부품 규격이므로 라벨·예시를 분리해야 한다 |
| A-3 | 3 | 상단 스펙 `물질 종류 N2` vs 물질/운전조건 `SiH4` — 같은 화면에서 충돌. A2 본문은 `SiH4 라인` 기준으로 서술 |
| A-4 | 1 | `SEC-01 · ENV_CRITICAL` 등 섹션 태그는 시스템에 없는 개념 |
| A-5 | 1 | `ISO 55000 표준 적합` — 검증 불가한 인증 주장 |
| A-6 | 7 | 상단 `ENG-SYSTEM: ONLINE / 플랜트 작업장 #2 / 최종 동기화` 밴드 — 없는 기능 |

## B급 — 다듬기

- 4·5·6·7: 본문은 Argus인데 로고만 `RF` 잔존. 로그인 화면만 `AR`로 맞다
- 4·5·6·7 푸터: `© 2025 ReplaceFlow Ecosystem` → Argus
- `VER 2.4.0-ENG` — 실제 버전과 무관한 표기
- 5: 화면명이 `엔지니어 메인`인데 안전관리자 내비 아래 있음(S-2와 동일 원인)

## 잘 나온 것 (그대로 유지)

- **3번(E_04) 항목별 편집 UI** — `ITEM #01~#04` + 항목별 삭제 + `항목 추가`가 A1/A2/A3 각각에 붙은 구조는 계약 그대로다. 이 화면이 이번 프로젝트의 핵심 차별점이고, 가장 정확하게 구현됐다
- **4번(S_02) 읽기 전용 배지** — 안전관리자는 AI 결과물을 수정할 수 없다는 규칙이 UI에 드러남
- 4번 `체크리스트 게이트 없이 승인/거절+사유로 처리합니다` — 범위 결정이 화면에 정확히 반영됨
- 5번 엔지니어 KPI 4종(작성 중·진행 중(AI)·승인 대기·반려·보완) — 스키마와 **정확히 일치**
- 6번 안전관리자 KPI 4종 + 거절 사유 TOP5 — 스키마와 일치(부제 문구만 제거하면 됨)
- 7번 상태 필터 탭 합계(1+2+1+1+1=6) — 산수 맞음
